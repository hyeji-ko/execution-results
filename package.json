{
  "name": "execution-results",
  "version": "1.0.0",
  "description": "전사 신기술 세미나 실행계획 웹앱",
  "main": "index.js",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && gh-pages -d dist"
  },
  "dependencies": {
    "firebase": "^10.7.1"
  },
  "devDependencies": {
    "vite": "^5.0.0",
    "gh-pages": "^6.0.0",
    "dotenv": "^17.2.2"
  }
}
